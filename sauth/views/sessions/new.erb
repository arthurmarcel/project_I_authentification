<h1>Log in</h1>

<% if @msg %>
	<p><%=@msg%></p>
<% end %>

<% if @errors %>
	<p>Log in failed for the next reasons :</p>
	<ul>
		<% @errors.each do |field, err| %>
			<li>
				<img src="/img/err.png" alt="puce" align="absmiddle" style="padding-right:10px;padding-top:1px;" />
				<%= field %> <%= err %>
			</li>
		<% end %>
	</ul>
<% else %>
	<p>Enter your login & password to log in</p>
<% end %>

<form action="/sauth/sessions" method="post">

	<div class="field">
		<label for="session_login">Login</label><br />
		<input id="session_login" name="login" size="30" type="text" value="<% if @login %><%=@login%><% end %>"/>
	</div>
  
	<div class="field">
		<label for="session_password">Password</label><br />
		<input id="session_password" name="password" size="30" type="password" />
	</div>
  
	<% if !@app.nil? %>
		<input id="app" name="app" value="<%=@app%>" size="30" type="HIDDEN" />
	<% end %> 	
	
	<% if !@origin.nil? %>
		<input id="origin" name="origin" value="<%=@origin%>" size="50" type="HIDDEN" />
	<% end %> 	
	
	<% if !@secret.nil? %>
 		<input id="secret" name="secret" value="<%=@secret%>" size="50" type="HIDDEN" />
	<% end %> 	
  
  <div class="actions">
    <input name="commit" type="submit" value="Log in"/>
  </div>
  
</form>

<br/>
<a href="/sauth/users/new">Register</a>
